# CORM 项目真实性与价值潜力验证报告

**验证日期：** 2026-08-30  
**验证对象：** `CORM_Organ_Machine_Prototype_2026-08-30.zip`  
**压缩包 SHA-256：** `65eee659686cf06415fff268246be71a8d500b889da2fe19d31c12e4ff8589fa`

## 一、结论

本项目不是空壳、静态演示或伪造结果集合。它是一套能够执行的 CORM（Contract-Organized Regenerative Machine）研究原型，核心代码、测试、实验、逐周期轨迹和结果数据之间存在真实的计算链路。压缩包完整性、清单哈希、测试套件以及全部 12 组实验均通过独立复验；在与归档环境不同的操作系统、CPU 架构和 Python 版本上，除运行时间、平台信息及浮点拟合末位外，稳定结果与归档一致。因此，项目在“原型确实存在、主要逻辑实验确实运行、归档结果可以重现”三个层面具有较高真实性。

项目在研究原型层面具有中高含金量。最有价值的不是某个加法器或某次故障恢复，而是它尝试建立一种清晰的系统抽象：**程序的持续身份由局部语义契约定义，物理实现可以在运行时更换；恢复目标是寻找当前资源条件下可容纳的任意精确等价实现，而不是复原原蓝图。** 代码已经把契约综合、精确等价检查、故障反向索引、影子分配、形态切换、局部证书树更新和完整组件轮换串成统一流程。这一组合具备继续发展为新型可验证重构运行时的潜力。

但项目目前不能被认定为“已经达到图灵奖级成果”，也不能被认定为已经证明真实硬件能够在损伤中持续计算。独立反证发现两个关键问题：

1. 程序求值没有经过物理 Cell 映射。即使把全部活动 Cell 标记为故障且不修复，逻辑输出仍然正确；所以现有实验验证的是“逻辑形态替换与资源账本的一致性”，不是故障物理基底上的真实执行。
2. 证书树存在实现错误。初始化和部分修复后的根哈希不等于从全部叶子正确重算的根；现有 `certificates_valid` 审计只检查单个器官证书，没有检查证书树。完整轮换后因所有叶路径均被更新，根才恢复一致。

此外，压缩包没有作者、许可证、代码仓库、提交历史、签名、论文投稿或可信时间戳。因而可以验证项目内容和实验的真实性，不能验证作者身份、知识产权归属和学术优先权。最终判断是：**这是一个有真实技术内容、方向值得继续投入的严肃原型；它具备形成高价值成果的潜力，但当前证据只支持“有潜力”，不支持“已完成图灵奖级贡献”。**

## 二、验证口径

本次验证把“真实性”和“含金量”拆成五个互不替代的层面：

| 层面 | 要回答的问题 |
|---|---|
| 归档完整性 | 压缩包是否损坏，清单是否覆盖文件，文件是否与打包时一致 |
| 可复现性 | 测试和实验能否在独立环境重新运行并得到相同稳定结果 |
| 主张真实性 | 代码是否真的执行了所声称的综合、验证、故障定位和替换流程 |
| 来源真实性 | 作者、时间、权属、公开发表和原创优先权是否有独立证据 |
| 价值潜力 | 与现有工作相比是否形成有意义的技术组合，能否发展为基础性成果 |

图灵奖判断采用 ACM 的正式标准作为约束：A.M. Turing Award 用于表彰“对计算领域具有持久重要性的重大贡献”，而不是奖励刚完成的单个原型。[ACM A.M. Turing Award 官方说明](https://amturing.acm.org/?pg=awards.html)

## 三、验证过程与结果

### 3.1 归档与清单

- ZIP 完整性测试通过，无 CRC 或解压错误。
- 压缩包共有 52 个条目，其中 47 个普通文件、5 个目录。
- `VERIFICATION_MANIFEST.json` 管理除清单自身之外的全部 46 个普通文件；没有漏列或多列。
- 46 个受管文件的 SHA-256 和字节数全部与清单一致。
- 清单、源码、结果、CSV 轨迹和图表形成同一归档快照；但清单是包内自签式清单，没有外部签名或可信时间戳，能证明内部一致性，不能单独证明来源。

### 3.2 独立运行环境

归档结果记录的环境为 Linux 6.18.35、x86-64、Python 3.13.5。独立复验环境为 macOS 26.3.2、ARM64、Python 3.14.6，依赖版本为 NumPy 2.5.2、Matplotlib 3.11.1、pytest 9.1.1。

测试套件结果为：

```text
..........                                                               [100%]
10 passed in 0.19s
```

随后完整运行 `run_all.sh`。12 个实验阶段全部返回成功，聚合结果再次得到 `all_experiments_success=true`。逐 JSON 字段比较显示：去除运行时间、进程号、平台元数据后，绝大多数稳定字段完全一致；缩放实验的两个斜率只在浮点末位有差异，例如全局斜率由 `0.9999999999999998` 变为 `1.0`。`vm_trace.csv` 和 `rule110_trace.csv` 逐行一致。该结果说明核心实验具有确定性和跨环境可复现性。

### 3.3 主要结果复验

| 实验 | 独立复验结果 | 能直接支持的结论 |
|---|---:|---|
| 四输入函数空间 | 65,536/65,536 个函数均有至少两个不同指纹的精确形态；196,601 个形态通过穷举 | 多形态综合不是少数手工函数特例 |
| 随机六输入函数 | 1,000 个函数、3,000 个形态全部通过 64 行真值表检查 | 综合方法可扩展到更宽但仍有界的接口 |
| 通用网表编译 | 12 个 4,096 门 DAG 全部成功；三个阶段共比较 37,748,736 个输出位，零差异 | 编译器不是只支持项目内置算术电路 |
| 8 位加法/乘法/ALU | 初始、20% 随机 Cell 故障修复后、完整轮换后均穷举正确 | 局部等价形态替换保持组合逻辑语义 |
| 64 位乘法器 | 12,288 个器官、96,254 个初始活动 Cell、4,812 个永久故障、3,567 个器官重配成功 | 资源账本和局部契约流程可承载较大软件模型 |
| 局部容量分离 | 原 20-Cell 蓝图在 7-Cell 局部预算下失败；7-Cell Shannon 形态成功 | 存在“蓝图不可行、等价形态可行”的有限见证 |
| 缩放 | 固定损坏 8 个器官时，再生量始终为 56 Cell；全局规模代理量由 224 增至 28,672 | 在该同构实验族中，局部替换工作不随总器官数增长 |
| 故障比例 | 1%、5%、10%、20%、30% 各 5 个种子，25/25 成功 | 在三倍左右冗余基底和当前故障模型下具备恢复能力 |
| 32 位乘法器轮换 | 3,072/3,072 器官改变形态，原始活动 Cell 残留 0，边集合 Jaccard 为 0 | 逻辑形态和资源映射能够完成全量替换 |
| 有状态累加器 | 2,000 周期逐周期匹配参考状态，初始计算/状态 Cell 残留均为 0 | 外部同步状态模型可与形态轮换并行推进 |
| Rule 110 | 1,024 位、2,048 轮、2,097,152 次位更新全部匹配参考实现 | 能精确承载有界环上的 Rule 110 局部规则 |

Rule 110 的通用性来源于 Cook 对无限背景构造的证明；本项目正确地把自身结果限定为有限环上的局部规则执行，没有把有限实验等同于完成通用性构造。[Cook, *Universality in Elementary Cellular Automata*](https://www.complex-systems.com/pdf/15-1-1.pdf)

## 四、代码与结果为什么不是静态伪造

核心链路经源码审查可还原如下：

1. `corm/core.py` 从真值表分别生成 SOP、ANF、Shannon 形态；若结构碰撞，则生成语义保持的冗余分区形态。
2. 每个候选都由 `Contract.verify_variant` 对局部全部输入穷举，而不是读取预置的成功标志。
3. `corm/compiler.py` 把拓扑有序的 `NOT/BUF/AND/OR/XOR/MUX` 网表逐门编译为器官网络。
4. `corm/runtime.py` 用 `cell_owner` 反向索引定位故障器官，分配影子 Cell，选择可行形态，重新验证契约，再更新活动形态、端点和证书路径。
5. `run_experiments.py` 在修复前、修复后和完整轮换后重新求值。通用 DAG 的参考结果由独立的 `BooleanNetlist.evaluate_bits` 产生；算术结果由 NumPy 或 Python 整数运算产生。
6. 结果文件只在实验完成后写出，没有被核心求值或成功判定读回。

这一证据链足以排除“结果 JSON 预先写好、实验脚本只打印成功”的情况。复跑得到相同的故障数、修复数、形态数、状态轨迹和最终摘要，也进一步支持实验数据确由代码生成。

需要降级的一处表述是“每个契约都有多个独立综合形态”：65,536 个四输入函数中有一个函数使用 `redundant_partition` 兜底，该形态由已有形态语义保持地派生，并非独立综合算法产生。它仍是不同结构的精确实现，但“全部均独立综合”不完全成立。

## 五、独立反证与关键限制

### 5.1 物理 Cell 没有参与实际求值——关键限制

独立测试建立一个 4 位乘法器，输入 `11 × 13`，先得到 143；随后把运行时全部活动 Cell 标记为故障，不执行修复，再次求值仍得到 143。运行时审计能够发现存在故障活动 Cell，并把 48 个器官加入待修复集合，但 `Program.evaluate_scalar` 只按当前逻辑 `Variant` 求值，不读取 Cell 映射、故障集合或物理连线。

因此，以下内容已经验证：

- 逻辑形态是精确等价的；
- 资源分配不会重复占用 Cell；
- 修复后活动映射避开已知故障 Cell；
- 形态、端点和资源账本可以完成轮换。

以下内容尚未验证：

- 计算确实由所分配的 Cell 和物理边执行；
- Cell 故障会在修复前真实破坏计算；
- 影子器官构建期间旧器官继续提供正确服务；
- 原子切换在并发、时序、电气层面成立。

换言之，当前模型是“逻辑程序 + 物理资源账本”的耦合模拟，不是物理基底执行器。项目的核心思想仍然有价值，但“在结构死亡中持续计算”只能作为未来硬件目标，不能作为本原型已经证实的事实。

### 5.2 证书树根不一致——已证实缺陷

`CertificateTree.recompute_all` 按节点编号逆序处理内部节点，而树的父节点编号大于子节点编号。这会导致根节点先于内部子树计算。独立递归重算得到：

| 时点 | 存储根与正确全树根是否一致 |
|---|---|
| 初始化后 | 否 |
| 20% 故障并部分修复后 | 否 |
| 所有器官完成轮换后 | 是 |

完整轮换会触及每个叶到根路径，所以最终根偶然被逐步修正；这不能修复初始化和部分更新阶段的错误。现有 `audit()['certificates_valid']` 实际只验证 `contract_hash + variant_fingerprint` 形式的单器官哈希，没有从叶子重算并核对证书树根。因此归档中的 `certificates_valid=true` 不能证明证书树有效。

此外，单器官证书不绑定物理 Cell 映射、布线、代次或故障状态。它证明的是逻辑形态与契约的对应关系，不是“这个物理部署正确实现了该形态”。这与项目声称的最终机器检查、物理可信链仍有明显距离。

### 5.3 状态轮换是标识轮换，不是状态 Cell 执行

累加器和 Rule 110 的真实状态保存在 Python 整数 `self.state` 中；`state_cells` 只保存 Cell 编号。迁移操作替换编号并退役旧编号，没有把每一位值编码到 Cell 对象并从新 Cell 读回。因此逐周期语义正确是真实的，但“状态 Cell 携带并迁移状态”目前是抽象记账，不是存储层模拟。

### 5.4 缩放与故障实验的解释边界

- 缩放实验中的“全局重编译量”直接设置为当前活动 Cell 数，没有实现和运行一个全局重编译基线。它证明的是所定义工作量的结构分离，不是实测速度优于成熟全局工具链。
- 固定 8 个同构全加器受损时，每个最小形态固定为 7 Cell，所以 56 Cell 常数结果符合构造；仍需在异构契约、拥塞和有限局部半径下验证一般性。
- 故障比例实验使用约三倍容量因子，修复默认可搜索整个二维基底；30% 成功不能外推为任意局部容量、相关故障、布线断裂或持续在线负载下都成功。
- 64 位乘法器只做局部契约穷举和修复前后各 16 个顶层随机乘积。项目已在自身限制文档中承认无法穷举 `2^128` 输入，这一点没有虚假夸大。
- “原子切换”是单线程 Python 状态替换，不包含并发访问、时钟域、亚稳态、时序收敛、功耗和信号完整性。

### 5.5 工程可复现性缺陷

README 给出的 `python3 -m pip install -e .` 在干净隔离环境中失败。setuptools 把平铺目录中的 `corm`、`results`、`figures` 识别为多个顶层包并拒绝自动发现。手动安装三项依赖并以源码目录作为 `PYTHONPATH` 后，测试和全部实验正常运行。该问题不否定算法结果，但说明当前发布包没有达到开箱即用的工程质量。

## 六、来源、署名与原创性核验

### 6.1 能确认与不能确认的来源事实

能确认：

- 归档时间、清单日期和文件时间一致；
- 清单覆盖完整，内容未在验证前发生包内不一致；
- 精确项目名称、算法名称和多条独特代码短语在本次公开网页检索中未发现同名公开副本。

不能确认：

- 谁是作者或权利人；
- 代码何时首次产生；
- 是否由一个或多个人独立完成；
- 是否已投稿、同行评审或对外公开；
- 是否拥有使用和再发布权；
- 学术优先权是否属于该压缩包持有人。

原因是包内没有 `LICENSE`、作者信息、Git 历史、远程仓库地址、签名、DOI、预印本或机构证明。精确名称未检索到公开结果只能说明名称可能尚未公开，不能证明科学思想首创。

### 6.2 与近邻工作的关系

| 近邻工作 | 已有能力 | CORM 的可能差异 | 对原创性判断的影响 |
|---|---|---|---|
| MIT Amorphous Computing | 大量近同质、不可靠、未知连接的单元通过局部机制形成稳健全局行为 | CORM 用显式布尔契约、多精确形态和确定性热切换组织器官 | “同质单元、自组织、自修复、形态发生”本身不是新点。[MIT Amorphous Computing](https://groups.csail.mit.edu/mac/projects/amorphous/white-paper/amorph-new/amorph-new.html) |
| NASA 演化式 FPGA 故障恢复（2003） | 在故障 FPGA 上演化新的逻辑与路由配置，并在软件 FPGA 模型中恢复 100% 正确的解码器 | CORM 不依赖在线遗传搜索，而是选择预先精确验证的局部形态 | “损坏后寻找不同配置恢复同一功能”已有明确先例。[NASA NTRS 20030018874](https://ntrs.nasa.gov/citations/20030018874) |
| UCF Competitive Reconfiguration（2008） | 设计时生成“功能相同、物理不同”的 FPGA 配置群；运行时竞争修复，并在 Virtex II Pro 上验证 | CORM 强调有界接口契约、局部形态谱、证书路径和无蓝图资源见证 | 该工作与 CORM 的“功能身份不等于物理蓝图”高度重合，是原报告遗漏的关键先例。[UCF 博士论文](https://stars.library.ucf.edu/etd/3499/) |
| Self-Organising Digital Circuits（2026） | 拓扑掩码 Transformer 学习 LUT 功能，局部策略在固定连线上恢复并找到功能等价配置 | CORM 无训练、局部接口可穷举精确、软件规模更大，并改变局部门级形态和 Cell 映射 | CORM 的精确性和确定性形成有效差异；但前者是真正的局部学习策略，CORM 仍是中央调度器。该论文也明确承认随机拓扑算术难以精确收敛、规模为数百门。[arXiv:2608.02606v2](https://arxiv.org/html/2608.02606) |
| Proof-Carrying Hardware | 已研究用可检查证明认证可重构硬件属性和功能等价，并做过真实可重构硬件原型 | CORM 把契约证书放进故障修复和形态轮换流程 | “运行时重构 + 功能等价证书”也不是从零出现；CORM 的差异需要由整体模型和复杂度结果建立。[Paderborn 2021 论文/学位工作](https://digital.ub.uni-paderborn.de/hs/content/titleinfo/6079251) |
| CompCert | 用 Coq 机器检查证明编译前后语义保持 | CORM 的目标是反复运行时替换而非一次编译 | 语义保持原则成熟；CORM 有价值的方向是把它扩展到持续物理重构。[CompCert 官方手册](https://compcert.org/man/manual001.html) |

项目报告列出的三篇 2026 年近邻工作均可核验存在：Self-Organising Digital Circuits、FPGA SoC 分层数字孪生 EIR，以及契约驱动 RTL 演化。[EIR, arXiv:2607.12298](https://arxiv.org/abs/2607.12298)；[Spec-Driven Hardware Evolution, arXiv:2608.12684](https://arxiv.org/abs/2608.12684)

综合判断：CORM 的单个组成思想大多存在历史先例，当前最可能形成原创贡献的是以下组合，而不是某一个孤立部件：

> 有界局部语义契约 + 多个确定性精确形态 + 当前资源约束下的局部选择 + 影子切换 + 增量证书树 + 完整物理支持轮换 + 维持语义的复杂度度量。

这一组合具有论文级新意的可能性，但现有压缩包没有系统文献综述、严格新颖性证明或同行评审，不能据此确认首创。

## 七、项目的价值潜力

### 7.1 计算抽象价值

传统容错通常围绕“恢复已知正确状态或配置”展开。CORM 把稳定对象上移到语义契约：只要边界函数不变，内部结构和物理支持可以改变。这个抽象一旦严密化，会把程序、编译器和硬件之间的接口从“固定实现”改为“可验证实现集合”。其意义类似于把 ABI 从具体函数体中分离出来，但进一步允许底层实现因故障和资源变化持续重构。

这为一个清晰的新研究问题提供了载体：计算复杂度研究通常度量首次求值的时间和空间，CORM 试图度量在持续损伤下维持同一语义所需的局部工作。即使“语义再生复杂度”最终需要重新定义和证明，这个问题本身具有基础研究价值。

### 7.2 算法价值

CORM 把恢复问题分解为边界清晰的局部步骤：故障定位、候选精确性、资源可行性、局部放置、切换和证书更新。`cell_owner` 使故障定位不必扫描全部器官；有界接口使真值表证书可局部检查；平衡树使单次证书更新路径随器官数对数增长。该分解方式简单、可替换，也适合未来把穷举验证替换为 SAT/SMT、BDD 或机器检查证明，而不改变运行时接口。

7-Cell 对 20-Cell 的容量见证尤其有概念价值：它说明“恢复功能”与“恢复结构”在资源可行性上不是同一问题。当前见证规模小且人为构造，但它指出了一个可以被严格推广的理论方向——在自然架构模型下证明语义形态恢复相对于固定蓝图恢复的优势边界。

### 7.3 形式化方法价值

如果修复后的实现必须携带可独立检查的证据，运行时重构就不必完全信任综合器。综合器可以复杂甚至不可信，部署端只保留小型检查器。这条路线能把形式化编译、Proof-Carrying Hardware 与故障恢复结合起来。当前证书树有缺陷，且证书只是重新执行真值表检查后的哈希；但正因为接口已经出现，后续可以逐步替换为真实证明对象，而不必重写整个研究方向。

### 7.4 工程与应用价值

若能够落到真实可重构硬件，该模型最适合以下场景：

- 深空、核环境、无人设施等难以人工维护的长期系统；
- 需要局部动态重构、又不能全局停机的 FPGA/CGRA；
- 可用资源持续减少、但允许实现形态改变的老化芯片；
- 要求每次在线更新都可证明满足接口契约的安全关键加速器；
- 模块可以由不同综合器生成、由统一小型验证器接纳的开放硬件运行时。

它的潜在优势不是当前 Python 速度，而是把恢复成本限制在受损器官、候选形态和证书路径附近，从而让系统规模增长时避免全局处理。只有在物理执行、局部通信和真实放置布线完成后，这个优势才可能转化为工程收益。

### 7.5 平台价值

项目的代码边界已经较清楚：`Contract`、`Variant`、`Program`、编译器和运行时之间的职责分离合理；算术电路、随机 DAG、状态机和 Rule 110 复用同一套接口。这说明它有发展为实验平台的潜力，可用于比较不同形态综合器、修复策略、证明系统、故障模型和基底架构，而不必为每个案例重写模拟器。

## 八、达到基础性成果所需的决定性证据

以下不是一般性的“继续优化”，而是决定项目价值能否兑现的门槛：

1. **让物理基底参与执行。** 每个门输出必须由映射 Cell 计算，故障 Cell 必须真实阻断或污染信号；所有端到端检查从该执行器读结果。
2. **修复证书树并强化审计。** 正确自底向上初始化；每次更新后独立重算根；证书绑定逻辑形态、物理映射、布线、代次和故障排除状态。
3. **真实建模状态。** 状态位存放在 Cell 中，迁移必须执行读、复制、校验、切换，并能在迁移故障下注入错误。
4. **从中央参考调度器过渡到局部微内核。** 明确每个同质 Cell 的有限状态、局部消息、时钟/异步假设，证明仅用局部信息可以完成发育和恢复。
5. **完成机器检查证明。** 至少覆盖局部替换语义保持、证书检查器正确性、完整轮换不变量和状态迁移；这将把当前纸面推导提升为可信理论结果。
6. **建立自然模型下的严格分离。** 不能把全局基线直接定义为全部 Cell 数；需给出合理对手模型、下界和可复现实测基线。
7. **实现 FPGA/CGRA 原型。** 注入真实配置位、LUT、路由和时序故障，测量恢复时延、停机窗口、面积、功耗、拥塞和可用性，并与部分重配置、备用模块、演化修复等方法比较。
8. **补齐学术来源链。** 明确作者与许可证，建立公开仓库和签名版本，完整覆盖最接近的旧工作，发布论文和独立复现实验。

## 九、图灵奖级别判断

| 判断维度 | 当前状态 |
|---|---|
| 是否提出可能改变系统边界的抽象 | 是，语义契约替代固定物理实现具有基础性潜力 |
| 是否已有可执行而非纯概念证据 | 是，代码和实验可重现 |
| 是否证明真实故障基底持续计算 | 否，逻辑求值绕过 Cell 基底 |
| 是否有完整可信证书 | 否，证书树存在已复现错误，证书也不绑定物理部署 |
| 是否有机器检查理论 | 否 |
| 是否有真实硬件与工程指标 | 否 |
| 是否已证明相对既有方法的严格优势 | 否，只有有限见证和定义式缩放实验 |
| 是否有同行评审、独立采用和长期影响 | 否 |
| 是否可验证作者与学术优先权 | 否 |

按照 ACM“重大且具有持久重要性”的标准，一个当日归档的软件原型不可能已经具备“持久影响”的证据。即使上述技术缺口全部修复，也还需要多年独立复现、学术使用、硬件验证和领域影响才能讨论奖项级别。

最准确的定位是：**CORM 当前是一颗有分量的研究种子，不是已经成熟的图灵奖成果。** 它的上限可能很高，因为它触及程序身份、形式化证明和可重构物理系统的交界；它的当前可信级别仍受物理执行缺失、证书错误、先例重叠和来源链缺失限制。

## 十、最终评级

| 项目 | 评级 | 依据 |
|---|---|---|
| 压缩包内部完整性 | 高，已确认 | ZIP、46 个文件哈希和字节数全部通过 |
| 实验可复现性 | 高，已确认 | 跨 Linux/x86-64 与 macOS/ARM64 稳定结果一致，12 阶段和 10 项测试均通过 |
| 局部布尔形态精确性 | 高，已确认 | 全四输入函数空间、随机六输入函数及多类端到端穷举 |
| 逻辑形态替换语义保持 | 高，限当前组合逻辑模型 | 替换前后参考比较一致，形态指纹和原始 Cell 支持均改变 |
| 真实物理自修复计算 | 未证实 | 求值绕过 Cell 映射，全部活动 Cell 故障时输出仍正确 |
| 证书树正确性 | 不成立 | 初始化和部分修复后根哈希独立重算不一致 |
| 状态 Cell 迁移真实性 | 未证实 | 状态存于 Python 整数，Cell 只承担编号账本 |
| 来源、署名和权属真实性 | 无法验证 | 无作者、许可证、仓库历史、签名或发表记录 |
| 学术原创性 | 中等、待证明 | 整体组合可能有新意，但多个核心思想有直接先例，现有综述不充分 |
| 研究价值潜力 | 中高、条件性 | 抽象清晰、实验覆盖广；价值能否兑现取决于物理执行、形式证明和硬件验证 |
| 当前图灵奖级别 | 否 | 缺少持久影响、严格理论、真实系统、独立采用和来源证明 |

**最终结论：项目具备真实且不低的研究含金量，值得作为“可验证语义再生计算”方向继续推进；其逻辑原型和实验数据基本真实，但物理自修复、证书可信链、原创优先权和图灵奖级影响均未被现有压缩包证明。**
